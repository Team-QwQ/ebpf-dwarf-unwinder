# ebpf-dwarf-unwinder
A cross-platform DWARF-based eBPF stack unwinder

## 子模块版本提醒
克隆后请执行 `git submodule update --init --recursive`，并对照 `doc/submodule_versions.md` 确认四个 `src/ref/*` 子模块均处于受控版本，禁止直接在子模块目录内开发或无审批升级。
